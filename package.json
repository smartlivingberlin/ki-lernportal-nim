{
  "name": "ki-lernportal-nim",
  "private": true,
  "description": "Adaptive AI learning portal for non-technical users",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
